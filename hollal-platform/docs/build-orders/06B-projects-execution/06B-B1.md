# 06B-B1 — Generation engine

> حالة: **منفّذ** — يوليو 2026. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Generation engine: copy template items (إلزامي + قالب خدمات العرض), compute dates, translate roles, create esnad tasks with hierarchy+evidence, project copy editable without touching template; entity tasks visible via link.

## Spec reference

docs/specs/ — project generation · 06A-B2 templates

## Deliverables

- Copy template items with date/role translation
- Create hierarchical esnad tasks + evidence
- Editable project copy; entity tasks via link

## Required grep proofs

- `generate|ProjectGenerat`
- `template_item_id`
- `إلزامي`

## Tests

- Generation does not mutate template
- Hierarchy depth respected
- Entity-visible tasks only via portal link policy

## Definition of done (protocol)

1. commit message includes `06B-B1`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
