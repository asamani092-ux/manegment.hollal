# 00-B2 — Hierarchical permission rename + role #7

> حالة: **منفّذ** — يوليو 2026. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Hierarchical permission rename migration (old→new map, e.g. expenses.approve→finance.expenses.approve), update seeder/roles/middleware/policies/views, full route-protection test before/after. Add role #7 مدير الشراكات.

## Spec reference

docs/specs/ — permissions convention tab.section.action · role matrix

## Deliverables

- Migration mapping old permission strings → hierarchical tab.section.action
- Updated PermissionSeeder, RoleSeeder, policies, middleware, blades, permission_labels
- New role: مدير الشراكات (#7)
- Before/after route-protection tests

## Required grep proofs

- `expenses.approve → finance.expenses.approve (map present)`
- `مدير الشراكات in RoleSeeder`
- `permission_labels hierarchical keys`

## Tests

- Route protection matrix before/after rename
- Operational roles still gate correctly including مدير الشراكات
- Full suite green

## Definition of done (protocol)

1. commit message includes `00-B2`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
