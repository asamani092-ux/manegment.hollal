# 00-B4 — Core entities: organizations, programs, projects links

> حالة: **منفّذ** — يوليو 2026. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Core entities: organizations(+contacts), programs (skeleton), projects.partnership_id/program_id/kind + data migration (extract entities from entity_name, reverse relation, map 4 statuses→7-stage journey, mark orphan projects داخلي) with --dry-run + migration tests.

## Spec reference

docs/specs/ — organizations/programs/projects foundation · partnerships journey stages

## Deliverables

- organizations + contacts models/migrations
- programs skeleton
- projects.partnership_id / program_id / kind + data migration
- Artisan migration command with --dry-run
- Status map 4→7-stage; orphan projects marked داخلي

## Required grep proofs

- `partnership_id|program_id|kind on projects migration`
- `--dry-run in migration command`
- `organizations table migration`

## Tests

- Dry-run does not mutate DB
- Entity extraction from entity_name covered
- Orphan projects marked داخلي
- Full suite green

## Definition of done (protocol)

1. commit message includes `00-B4`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
