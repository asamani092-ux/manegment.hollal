# 00-B3 — Notification channels core

> حالة: **منفّذ** — يوليو 2026. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Notification channels core: database + queued mail for every critical notification; SMTP settings screen (test-send button); notification preferences groundwork.

## Spec reference

docs/specs/ — notifications · docs/DEPLOYMENT.md SMTP ops

## Deliverables

- Database + queued mail channel for critical notifications
- SMTP settings screen with test-send (no fake success)
- Notification preferences groundwork tables/UI stubs

## Required grep proofs

- `ShouldQueue on critical mailables/notifications`
- `test-send action in SMTP settings Livewire`
- `notification_preferences (or equivalent) migration/model`

## Tests

- Critical notification creates database row and queues mail
- Test-send fails honestly when SMTP misconfigured
- Full suite green

## Definition of done (protocol)

1. commit message includes `00-B3`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
