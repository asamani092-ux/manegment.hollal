# 00-B6 — Settings core + category trees

> حالة: **منفّذ** — يوليو 2026. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Settings core (platform_settings key/value + old/new value logging) + trees: expense categories, revenue categories, asset categories.

## Spec reference

docs/specs/ — settings · finance categories

## Deliverables

- platform_settings key/value store
- Old/new value audit logging on setting changes
- Expense / revenue / asset category trees

## Required grep proofs

- `platform_settings migration`
- `settings.changed|setting.updated audit action`
- `expense_categories|revenue_categories|asset_categories`

## Tests

- Setting change writes old/new audit log
- Category CRUD soft-delete safe
- Full suite green

## Definition of done (protocol)

1. commit message includes `00-B6`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
