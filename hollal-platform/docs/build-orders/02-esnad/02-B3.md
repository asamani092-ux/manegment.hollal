# 02-B3 — Recurring templates + workload + calendar

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Recurring templates + workload board (threshold from settings) + tasks calendar (shows approved leaves).

## Spec reference

docs/specs/ — esnad templates/workload/calendar

## Deliverables

- Recurring task templates
- Workload board with settings threshold
- Tasks calendar including approved leaves

## Required grep proofs

- `recurring|task_templates`
- `workload`
- `calendar`

## Tests

- Template generates next occurrence
- Workload flags over threshold
- Approved leaves appear on calendar

## Definition of done (protocol)

1. commit message includes `02-B3`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
