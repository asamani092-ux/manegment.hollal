# 02-B4 — Task hierarchy (5-level)

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Hierarchy support: parent_task_id (5-level), template_item_id — flat cards remain in esnad, tree in project page.

## Spec reference

docs/specs/ — esnad hierarchy · projects plan tree

## Deliverables

- parent_task_id with max 5 levels
- template_item_id link
- Flat cards in esnad; tree on project page

## Required grep proofs

- `parent_task_id`
- `template_item_id`
- `depth|level.*5|max.*5`

## Tests

- Reject depth > 5
- Esnad index remains flat cards

## Definition of done (protocol)

1. commit message includes `02-B4`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
