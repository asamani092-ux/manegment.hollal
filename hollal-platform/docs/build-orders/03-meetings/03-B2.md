# 03-B2 — Minutes archive + amendments + stale decisions

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Auto-archive approved minutes PDF to documents (سنة←شهر←اجتماع, read-only) + amendment flow (طلب→موافقة المدير→نسخة موسومة «معدَّل») + stale decisions surfacing.

## Spec reference

docs/specs/ — meetings minutes/documents

## Deliverables

- Auto-archive PDF under سنة/شهر/اجتماع in documents (read-only)
- Amendment flow with معدَّل stamp
- Stale open decisions surfacing

## Required grep proofs

- `معد.?ل|amended`
- `archive.*minutes|documents.*meeting`
- `stale.*decision|قرارات.*متأخر`

## Tests

- Approved minutes create document row
- Amendment requires manager approval
- Stale decisions listed

## Definition of done (protocol)

1. commit message includes `03-B2`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
