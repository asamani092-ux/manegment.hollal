# 05-B4 — Contracts + e-signature

> حالة: **منفّذ** — يوليو 2026. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Contracts: from quote, payment schedule, in-link e-signature (pad+name+timestamp+device, merged into PDF+hash) WITH download/sign/upload fallback; تعاقد = confirmed signed copy (+first payment if required).

## Spec reference

docs/specs/ — partnerships contracts

## Deliverables

- Contract from quote + payment schedule
- In-link e-sign + download/sign/upload fallback
- تعاقد = confirmed signed copy (+ first payment if required)

## Required grep proofs

- `e-?sign|signature`
- `payment_schedule`
- `hash`

## Tests

- Signed PDF hash stored
- Fallback upload path authorized
- تعاقد gate enforces first payment when required

## Definition of done (protocol)

1. commit message includes `05-B4`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
