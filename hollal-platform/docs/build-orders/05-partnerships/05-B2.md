# 05-B2 — Seven-stage pipeline

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Seven-stage pipeline (Kanban+list, stage forms, stage logs, stale alerts).

## Spec reference

docs/specs/ — partnerships pipeline

## Deliverables

- Kanban + list for 7 stages
- Stage forms + stage logs
- Stale alerts

## Required grep proofs

- `stage`
- `kanban|pipeline`
- `stale`

## Tests

- Stage transition writes stage log
- Stale alert when threshold exceeded

## Definition of done (protocol)

1. commit message includes `05-B2`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
