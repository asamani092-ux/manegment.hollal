# 05-B6 — Payments + invoice hook

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Payments schedule vs recorded, finance confirmation → auto revenue (once), late alerts; «إصدار فاتورة» hook to 04-B7.

## Spec reference

docs/specs/ — partnerships payments · 04-B7

## Deliverables

- Schedule vs recorded payments
- Finance confirmation → auto revenue once
- Late alerts + إصدار فاتورة hook

## Required grep proofs

- `payments`
- `إصدار فاتورة|issue.*invoice`
- `late|متأخر`

## Tests

- Double confirmation does not duplicate revenue
- Invoice hook creates/links tax invoice when 04-B7 present

## Definition of done (protocol)

1. commit message includes `05-B6`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
