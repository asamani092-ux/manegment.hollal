# 05-B5 — Unique link portal

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Unique link portal: full journey (browse programs, diagnosis form, quote accept/notes, contract, payments recording, later: tasks/evidence/reports), strict isolation+rate-limit+full activity logging.

## Spec reference

docs/specs/ — partner portal

## Deliverables

- Unique-link portal journey screens
- Strict isolation + rate-limit
- Full activity logging

## Required grep proofs

- `magic_link|unique.?link|partner.*portal`
- `throttle`
- `audit|activity.*portal`

## Tests

- Token A cannot access org B data (IDOR)
- Rate-limit on portal routes
- Activity log on key actions

## Definition of done (protocol)

1. commit message includes `05-B5`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
