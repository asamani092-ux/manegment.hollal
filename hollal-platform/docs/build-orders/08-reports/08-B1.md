# 08-B1 — Unified reports center

> حالة: **منفّذ** — يوليو 2026. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Unified center + monthly report + project dashboard report (Excel-matching indicators) + snapshots.

## Spec reference

docs/specs/ — reports

## Deliverables

- Unified reports center
- Monthly report + project dashboard report
- Snapshots

## Required grep proofs

- `reports`
- `snapshot`
- `monthly`

## Tests

- Indicators match Excel mapping fixtures
- Snapshot immutable after create

## Definition of done (protocol)

1. commit message includes `08-B1`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
