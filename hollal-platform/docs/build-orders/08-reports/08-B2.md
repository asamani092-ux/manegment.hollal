# 08-B2 — Impact/KPI/audit screens

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Final/visit reports polish + impact report + KPIs + audit-log screen (filters, export, read-only) + per-report derivation tests.

## Spec reference

docs/specs/ — reports/audit

## Deliverables

- Final/visit/impact reports polish
- KPIs
- Audit-log screen filters/export read-only
- Per-report derivation tests

## Required grep proofs

- `audit_logs`
- `KPI|kpi`
- `export`

## Tests

- Audit screen read-only (no update/delete routes)
- Derivation tests for each report type

## Definition of done (protocol)

1. commit message includes `08-B2`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
