# 01-B5 — Leaves, responsibilities, evaluation, onboarding/offboarding

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Leaves (minimal), المسؤوليات والمهام الوظيفية + official duties file (versioned, visible to all), periodic evaluation (score /5 per responsibility item, evaluator = executive manager, configurable to dept manager), onboarding/offboarding checklists → esnad tasks + auto account disable.

## Spec reference

docs/specs/spec-01-hr.md §5–§8

## Deliverables

- Minimal leave requests + approval
- Responsibilities list + versioned official duties file
- Periodic evaluation /5 per responsibility item
- Onboarding/offboarding → esnad tasks + auto disable account

## Required grep proofs

- `leave_requests|hr.leaves`
- `responsibilities`
- `onboarding|offboarding`
- `must_change_password|is_active.*false|disable`

## Tests

- Leave cycle request→approve
- Offboarding last step disables account
- Evaluation score bounds 1–5

## Definition of done (protocol)

1. commit message includes `01-B5`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
