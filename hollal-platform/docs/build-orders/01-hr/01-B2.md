# 01-B2 — Salary components + سلم الرواتب + overtime value

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Salary components (persistent monthly — تبقى حتى تُعدل/تُحذف) + سلم الرواتب editor + overtime hour value field.

## Spec reference

docs/specs/spec-01-hr.md §4

## Deliverables

- Persistent salary_components per employee
- سلم الرواتب editor
- Overtime hour value field (settings or component)

## Required grep proofs

- `salary_components`
- `overtime.*value|overtime_hour_value`
- `hr.salaries.manage`

## Tests

- Components persist until edited/deleted
- Unauthorized cannot manage salaries

## Definition of done (protocol)

1. commit message includes `01-B2`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
