# 11-B1 — Remaining settings sections

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Remaining settings sections (general/logo, timings incl. stale/aging thresholds, unique-link policy, financial, evaluation cycle, monthly attendance days, backup status+manual, maintenance mode) — each read live where used + old/new logging.

## Spec reference

docs/specs/ — settings · 00-B6 platform_settings

## Deliverables

- All remaining settings sections
- Live reads at usage sites
- Old/new logging on every change

## Required grep proofs

- `platform_settings`
- `maintenance`
- `stale|aging|attendance.*days`

## Tests

- Each setting key read by at least one feature test
- Change logs old/new values

## Definition of done (protocol)

1. commit message includes `11-B1`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
