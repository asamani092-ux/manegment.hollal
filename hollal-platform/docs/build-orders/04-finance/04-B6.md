# 04-B6 — Budgets + strict financial reports

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Budgets aggregate screen (80/100% alerts) + strict financial reports (derivation-only + reconciliation tests).

## Spec reference

docs/specs/ — finance budgets/reports

## Deliverables

- Budgets aggregate screen
- 80%/100% alerts
- Strict derivation-only financial reports + reconciliation tests

## Required grep proofs

- `budget`
- `0\.8|80%|threshold`
- `reconcil`

## Tests

- Alert fires at 80% and 100%
- Report totals reconcile to source ledgers

## Definition of done (protocol)

1. commit message includes `04-B6`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
