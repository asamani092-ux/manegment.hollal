# 04-B3 — Custodies

> حالة: **معلّق** — لا يُنفَّذ إلا بتوجيه صريح. يخضع لـ [`PROTOCOL.md`](../PROTOCOL.md).

## Objective

Custodies: request (employee→executive approve or executive direct)→disburse→settle by invoices (category-linked)→match; reminders; block on offboarding.

## Spec reference

docs/specs/ — finance custodies

## Deliverables

- Custody request/approve/disburse/settle flow
- Invoice matching by category
- Reminders + offboarding block

## Required grep proofs

- `custod`
- `settle|تصفية`
- `offboarding.*custod|custody.*block`

## Tests

- Employee request requires executive approve
- Offboarding blocked while open custody
- Settle matches invoices

## Definition of done (protocol)

1. commit message includes `04-B3`
2. push origin/main + hash range
3. git status clean / up to date
4. `php artisan test` full suite summary pasted
5. grep proofs pasted
6. files created/updated listed
