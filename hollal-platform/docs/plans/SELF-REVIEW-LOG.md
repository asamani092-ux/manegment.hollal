# سجل المراجعة الذاتية — توجيه الإكمال

**الفرع:** `fix/final-gaps` (متابعة `fix/complete-ui`) · **التاريخ:** 2026-07-30  
**البوابة:** `NavSelfReviewTest` + `CompletionUiRoutesTest` + `LeaveRequestTest` · دور Super Admin

| التبويب | الأدوات المفتوحة ✓ | سجل تجريبي | إنجليزي مكشوف | الحالة |
|---------|---------------------|------------|----------------|--------|
| الرئيسية | الرئيسية | — | لا | ✅ |
| الموارد البشرية | دليل العاملين، عقود العاملين، سلم، مسيّرات، تقييم، مسؤوليات، حضور، إجازات، تهيئة/إنهاء | تقديم/اعتماد إجازة + خصم رصيد | لا | ✅ |
| إسناد | المهام، مهام الفريق، تقويم (+إجازات)، متكررة، لوحة الأحمال | — | لا | ✅ |
| الاجتماعات | الاجتماعات، القرارات المفتوحة | — | لا | ✅ |
| المالية | صرف، مسير، عهد، أصول، إيرادات، فواتير، ميزانيات، تقرير، مستندات مالية | — | لا | ✅ |
| الشراكات | جهات، رحلة؛ عقد الشراكة داخل الصفحة | — | أُصلح «hash»→«بصمة الملف» | ✅ |
| المشاريع | محفظة، برامج، قوالب، زيارات، قياس | — | لا | ✅ |
| المستندات | مستودع، قوالب، إدارة النسخ، سياسات، أرشيف محاضر | رفع نسخة دون محو القديمة | لا | ✅ |
| التقارير | مركز، أسبوعي، سجل نشاط | — | لا | ✅ |
| الأقسام والهيكلة | أقسام، هيكل، وظائف، لجان | — | مستوى عربي (إدارة/وحدة/وظيفة) | ✅ |
| الأدوار | أدوار، منح | — | لا | ✅ |
| إعدادات المنصة | عامة، مالية، SMTP | — | SMTP مسموح بالمعجم | ✅ |

**سلسلة الإنجاز:** نموذج→هجرة→خدمة→Livewire→مسار→قائمة — مكتملة للأدوات في COMPLETION-MATRIX (ما عدا مدمج مقصود داخل الأب).

---

## تدقيق ما قبل التجربة — 2026-08-01

فحص منطقي لكل الشاشات الجديدة (صلاحيات، دقة بيانات، استعلامات، أعمدة مقابل المخطط).

| # | الخلل | الأثر | الإصلاح |
|---|-------|-------|---------|
| 1 | `LeavesIndex::assertCanDecide` يسمح لمن يملك `hr.leaves.view-all` باعتماد إجازته | اعتماد ذاتي | `abort_if` على `employee_id === auth()->id()` + إخفاء الأزرار |
| 2 | `LeaveService::approve` بلا قفل | اعتمادان متزامنان يخصمان الرصيد مرتين | `lockForUpdate` على الطلب والملف + إعادة فحص الحالة |
| 3 | الرصيد يُفحص لكل طلب على حدة | تجاوز الرصيد بطلبات متتالية | الطلبات «مقدم» تحجز أيامها + منع تداخل الفترات |
| 4 | `JobsIndex` يختار `is_active` غير الموجود في `org_units` | خطأ SQL على MySQL عند وجود صفوف (SQLite يمرّره صامتًا) | استبداله بـ `manager_id`/`job_purpose` |
| 5 | `DocumentVersionsIndex` بلا `visibleTo` | تسريب عناوين نسخ مستندات خارج نطاق المستخدم | تطبيق النطاق على القائمة والقائمة المنسدلة والرفع |
| 6 | `AttendanceIndex::declareType` يكتب `check_in_at => now()` ويقبل `?? true` | إفساد وقت الحضور الفعلي + إقرار بلا تفعيل | إزالة الكتابة + توحيد الشرط على `?? false` |
| 7 | `HrLifecycleIndex` يستدعي `holds()` لكل صف | ‎2×n استعلام لكل صفحة | `holdsForMany()` — استعلامان ثابتان |
| 8 | لا فحص خادم لمنع إنهاء الذات/تكرار الإنهاء | تعطيل حساب المنفّذ أو مهام تسليم مكررة | `abort_if` + حارس `STATUS_TERMINATED` |
| 9 | `EvaluationsIndex` يصطدم بقيد الفريدة | استثناء SQL بدل رسالة | فحص مسبق + تحقق صيغة `YYYY-QN` |
| 10 | `TasksCalendar` يثق بقيمة `month` | استثناء Carbon → 500 | ارتداد آمن للشهر الحالي |
| 11 | رابط المحضر في الأرشيف يظهر لمن لا يملك `meetings.view` | 403 بعد النقر | حصر الرابط بـ `@can` |
| 12 | `ds-badge-info` / `ds-badge-warning` مستخدمة بلا تعريف CSS | شارات بلا لون | تعريفها + `ds-badge-muted` |
| 13 | أدوار (تنفيذي/مشاريع/مالية/شراكات) بلا `hr.leaves.request` | لا خدمة ذاتية للإجازات | إضافة الصلاحية للأدوار |
| 14 | الحزمة تتجاوز `memory_limit=128M` | `Premature end of PHP process` في التشغيل الكامل | `<ini name="memory_limit" value="512M"/>` في `phpunit.xml` |

**الاختبارات:** 430 ناجحة / 1909 تأكيد — منها `PreTrialHardeningTest` (6 اختبارات انحدار تغطي البنود 1، 3، 4، 5، 8).

---

## تحسينات التجربة والواجهة — 2026-08-01

| الأولوية | التحسين | الأثر |
|---|---|---|
| P0 | طي مجموعات الشريط الجانبي مع حفظ الحالة في `localStorage` وفتح المجموعة النشطة تلقائيًا | 50 عنصرًا لم تعد مفتوحة دفعة واحدة |
| P0 | حقل بحث فوري داخل الشريط يفلتر العناصر ويوسّع المجموعات المطابقة | الوصول لأي أداة بضغطتين |
| P1 | صف فلاتر موحّد (`ds-filters-row`) على شاشات الإجازات، التقييم، المسؤوليات، الحضور، النسخ، الأرشيف، العهد، الأصول، الإيرادات، المسيّرات، الزيارات، القياس، الوظائف، اللجان | نفس نمط «طلبات الصرف» في كل الشاشات |
| P1 | مكوّن `x-ds-status-badge` يوحّد ألوان الحالات العربية | الحالة تُقرأ لونًا لا نصًا فقط |
| P1 | `x-ds-empty-state` بأيقونة ورسالة بدل خلية فارغة | لا شاشات صامتة |
| P2 | توحيد النوافذ على `x-ds-modal` مع `aria-modal` وزر إغلاق موصوف وإغلاق بمفتاح Escape | إمكانية وصول وسلوك موحّد |
| P2 | بطاقات موبايل للإجازات والعهد والزيارات | لا تمرير أفقي على الجوال |
| P2 | `ds-ltr-num` على المبالغ والتواريخ في جداول سطح المكتب | أرقام لا تنكسر في RTL |
| P3 | `scope="col"` في رؤوس الجداول و`aria-label` لأزرار الأيقونات | قارئ الشاشة يربط الخلية بعنوانها |
| P3 | ترقيم صفحات مسيّرات الرواتب | لا قائمة غير محدودة |

## جرد أدوات ما قبل التجربة (UAT) — 2026-08-01

جُرد كل مسار في `routes/web.php` (76) وقُوبل بـ `config/navigation.php`.

| النتيجة | التفصيل |
|---|---|
| فجوة واحدة | `payroll.index` «الرواتب الشهرية» شاشة كاملة ومختبَرة (`PayrollAccessTest`, `LivewireIdorTest`) لكنها بلا مدخل تنقّل — أُضيفت تحت الموارد البشرية بصلاحية `hr.salaries.view`، وأُدرج المصطلح في `GLOSSARY-AR.md` والصف في المصفوفة |
| مسارات بلا مدخل بالتصميم | تفاصيل (`projects/{id}`, `programs/{id}`, `organizations/{id}`, `partnerships/{id}`, `users/{id}/profile`, `meetings/{id}/minutes`) · بوابات الشريك بالرمز · مسارات تنزيل وPDF · الدخول وتغيير كلمة المرور |
| ازدواج مقصود | `payroll-runs.index` مدرج في «الموارد البشرية» و«المالية» بصلاحيات مختلفة |
| التغطية | 65 صفًا في قائمة تقييم الأدوات تشمل التنقّل والدخول و11 تبويبًا والشاشات الفرعية |

الاختبارات بعد الإضافة: 431 اختبارًا / 1915 تحققًا — كلها ناجحة.

## بيانات التجربة (Demo Seed) — 2026-08-01

كانت القاعدة المحلية شبه فارغة (7 مستخدمين، مهمة واحدة، صفر مستندات/عقود/إجازات)، فكانت أغلب الشاشات تُفتح على حالة فراغ ولا يمكن اختبار التدفقات ولا الفلاتر ولا الصلاحيات.

| البذرة | ما تُعبّئه |
|---|---|
| `DemoHrSeeder` | ملفات وظيفية · 5 عقود · سلم رواتب ومكوناته · 12 راتبًا شهريًا · 3 مسيّرات وبنودها · 4 تقييمات · 5 مسؤوليات · 15 سجل حضور · 4 إجازات (منها معتمدة في الشهر الحالي تظهر بالتقويم) |
| `DemoFinanceSeeder` | 6 أصول و4 حركات · 6 إيرادات على 3 أشهر · 3 فواتير ضريبية بضريبة 15% مطابقة لبنودها · 5 طلبات صرف بكل مراحل الاعتماد · 4 عهد منها مسوّاة |
| `DemoPartnershipsProjectsSeeder` | 4 جهات وجهات اتصال · 4 شراكات تغطي 4 مراحل من الرحلة · عقدا شراكة وجدولا دفعات · عرضا سعر · 4 برامج وأسعارها · 3 مشاريع · 5 زيارات · نموذجا قياس |
| `DemoDocsStructureSeeder` | 10 مستندات (منها 4 سياسات) و5 نسخ · 4 قوالب · 3 لجان وأعضاؤها · شجرة هيكل من 9 عقد · 3 قوالب مهام متكررة · 3 محاضر معتمدة وقرار متأخر |
| `DemoOpsSeeder` | 12 مهمة بكل الحالات مع ملاحظات وسجل حالات · حضور الاجتماعات · 3 منح استثنائية · 5 تحديثات مشاريع · 3 تقارير أسبوعية ولقطتان · 5 تنبيهات غير مقروءة للمدير |

`DemoTrialSeeder` ينادي الخمسة، ويفعّل `attendance_enabled` لمستخدمي التجربة، ويسند المستخدمين إلى بطاقات وظيفية ليظهر عدد الأعضاء في شجرة الهيكل. كل بذرة idempotent (تشغيلها مرتين لا يضاعف الصفوف).

**اختبار حارس جديد** `tests/Feature/DemoDataSmokeTest.php`: يبذر القاعدة كاملة ثم يفتح **51 مسار قائمة + 7 صفحات تفاصيل** ويتحقق من أن 20 جدولًا معبّأ — لأن الجدول الفارغ يخفي أخطاء استعلام البنود (هكذا اختفى خطأ `JobsIndex` سابقًا). المجموع: 434 اختبارًا / 2012 تحققًا، كلها ناجحة.

**حدود معروفة بالتصميم:** مسارات ملفات المستندات التجريبية وهمية فالتنزيل عليها يفشل · دفعة عقد الشراكة تُركت «بانتظار تأكيد المالية» لأن التأكيد هو ما يُنشئ الإيراد المرتبط.

**تسليم نهائي للوكيل الرئيسي:** جاهز بعد جولة عبدالله اليدوية. **لا دمج main** من هذا الفرع حتى المراجعة النهائية.

**ROADMAP (خارج النطاق):** KPI cache · API برامج · واتساب · CSP · ZATCA Phase B · قوالب needs_review

---

## الدفعة 0 — GEN — `fix/report-round-1`

| أمر | ما نُفّذ | تحقق |
|---|---|---|
| GEN-1 | المستمع يقبل `toast` و`ds-toast` · toast دخول · زر «جاري تسجيل الدخول» · `<x-ds-toast />` في guest | AuthTest + شاشة الدخول |
| GEN-2 | روابط عميقة `?open=` للمهمة/الصرف/العهدة/الإجازة + ملف الشراكة | TaskNotificationTest |
| GEN-3 | زر رجوع في partnership/organization/program show | بصري |
| GEN-5 / DOC-1 | `DownloadHeaders` + عنوان المستند في التنزيل | DownloadHeadersTest |
| GEN-6 | طي الشريط على سطح المكتب + `localStorage` | CSS/JS |
| GEN-7 | نسيت كلمة المرور (جوال/بريد) بسائق log | ForgotPasswordTest |

لا دمج `main`.

---

## الدفعة 1 — HR — `fix/report-round-1`

تعارض موثّق: spec-01 §3 يقول التجديد ينشئ سجلًا جديدًا؛ أمر الجولة 1 (HR-8) يمدد نفس العقد + `renewal_history`. نُفّذ أمر الجولة 1. الملحق 1.1 لا يحسم البند.

تغيير سلوك عام (التوقيع كما هو): `OffboardingService::offboard()` يبدأ المهام ولا يعطّل الحساب — التعطيل في `complete()`.

| أمر | ما نُفّذ | تحقق |
|---|---|---|
| HR-1 | زر «تعديل» نصي في جدول الدليل + زر تعديل في الملف الوظيفي | ReportRound1HrTest |
| HR-2 | ربط سلم/درجة في الملف · الراتب مشتق · خصم ثابت للنظامي فقط · سبب إلزامي للمتغير | SalaryService + PayrollRunService |
| HR-3 | شرح الحضور + تفعيل لكل موظف · المسير يسحب الإضافي من الحضور | Attendance + PayrollRunTest/ReportRound1HrTest |
| HR-4 | درجات /5 لكل مسؤولية · اطلاع وتعليق بعد النشر · عرض في الملف | EvaluationsIndex + الملف |
| HR-5 | 4 مهام إنهاء في إسناد · الموانع تمنع الإغلاق · التعطيل آخرًا | HrLifecycleTest / Custody / Asset |
| HR-6 | نافذة تفاصيل المسير + بند متغير بسبب + قبول/رفض مالية | PayrollRunsIndex |
| HR-7 | عمود عدد الموظفين في سلم الرواتب | PayScalesIndex |
| HR-8 | ساري/منتهي/مكتمل/معلق · معاينة PDF · رسائل رفع · تجديد يمدد + سجل | ContractService |
| إجازات | طلب←إشعار←رصيد قائم (LeaveRequestTest) | تحقق دون أمر منفصل |

لا دمج `main`.
