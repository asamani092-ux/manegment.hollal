# نتائج تجربة القبول — المرحلة 2

**التاريخ:** 2026-09-04  
**البيئة:** محلية بعد `migrate:fresh --seed`  
**المستخدم:** مدير النظام — `0500000000`  
**المنهج:** جلسة HTTP موثّقة (كوكي) + فحوصات عدّادات ونماذج — بدون واجهة رسومية

---

## خلاصة

| الحكم | العدد |
|--------|------|
| يعمل | 7 تبويبات |
| جزئي | 4 تبويبات |
| لا يعمل | 0 |

بيانات البذرة كافية لمعظم الشاشات. أبرز المتبقيات: عناوين تبويب المتصفح (`A1`) على 4 صفحات موارد بشرية/أرشيف، و`GET /logout` ما زال `405` (`A8`)، وتبويب الصرف يفتح على «طلباتي» فارغ للمدير رغم وجود 6 طلبات في «جميع الطلبات».

---

## مصفوفة 11 تبويبًا

| # | التبويب | الحكم | المسارات الحرجة | كفاية البيانات | ملاحظات |
|---|---------|--------|------------------|----------------|----------|
| 1 | موارد بشرية | جزئي | `/users` 200 · `/attendance` 200 · `/leaves` 200 · `/evaluations` 200 · `/payroll`→`/payroll-runs` 200 | كافية (حضور، إجازات، دورة تقييم + تقييمات موظفين، وردية) | `<title>` = «الرئيسية» لـ الحضور/الإجازات/التقييم (`A1`). عمود الوردية يظهر «صباحية» في عينات. |
| 2 | إسناد | يعمل | `/tasks` 200 | كافية | قائمة ومكوّن Livewire يظهران |
| 3 | اجتماعات | جزئي | `/meetings` 200 · `/meetings/{id}/minutes` 200 · `/meetings/archive` 200 | كافية (أجندة لجميع الاجتماعات، مسؤول ∈ حضور، تعديل محضر معلّق) | أرشيف المحاضر بلا عنوان تبويب صحيح (`A1`). طلب تعديل «معلق» ظاهر في الأرشيف. |
| 4 | مالية | جزئي | `/expenses` 200 · `/custodies` 200 · `/revenues` 200 · `/accounting-reports` 200 | كافية (≥4 طلبات صرف بحالات متمايزة + فئات) | الافتراضي «طلباتي» فارغ للمدير؛ البيانات في تبويب «جميع الطلبات» (`UX`). |
| 5 | شراكات | يعمل | `/organizations` 200 · `/partnerships/pipeline` 200 · `/partnerships/{id}` 200 | كافية | ملف الشراكة ورحلة الأنابيب يظهران |
| 6 | مشاريع | يعمل | `/projects` 200 · `/projects/{id}/execution` 200 | كافية (3 مشاريع عربية + 5 زيارات) | أسماء عربية ظاهرة في القائمة |
| 7 | مستندات | يعمل | `/documents` 200 | كافية | معاينة ظاهرة في HTML؛ `uploader.org_unit_id` مُحمَّل في الاستعلام (`H1` مُعالَج في الكود) |
| 8 | تقارير | يعمل | `/reports/center` 200 · `/reports` 200 · `/reports/audit-log` 200 | كافية | |
| 9 | هيكلة | يعمل | `/structure/org-tree` 200 | كافية | |
| 10 | أدوار | يعمل | `/settings/grants` 200 | كافية | |
| 11 | إعدادات | يعمل | `/settings` 200 | كافية | نموذج الإعدادات يظهر |

---

## تحقق بنود التقرير السابق

| رمز | الحالة في المرحلة 2 | دليل |
|-----|---------------------|------|
| H1 | مُصلح في الكود | `DocumentsIndex` يحمّل `uploader:id,name,org_unit_id`؛ رافعو البذرة لديهم `org_unit_id` |
| H2 | مُصلح في الكود | `DocumentPolicy::delete` يستدعي `canAccess` قبل السماح |
| A1 | مفتوح جزئيًا | صفحات بلا `title`: حضور، إجازات، تقييم، أرشيف محاضر |
| A2 | مغلق بالبذرة | `Meeting` بأجندة غير فارغة = كل الاجتماعات (4/4) |
| A3 | مغلق بالبذرة | بنود بمسؤول خارج الحضور = 0 |
| A4 | مغلق بالبذرة | `ExpenseRequest` = 6 بحالات pending/approved/paid/rejected |
| A5 | مغلق بالبذرة | `EvaluationCycle`=1 · `EmployeeEvaluation`=6 · `PeriodicEvaluation`=4 |
| A6 | مغلق بالبذرة | `MeetingAmendment`=1 بحالة معلّق |
| A7 | جزئي | `lang="ar"` وعنوان عربي على `/login`؛ حقول الدخول بلا `title` عربي لرسائل HTML5 |
| A8 | مفتوح | `GET /logout` → **405** (POST فقط) |
| M1 | يبدو مغلقًا | خيار «كل الإدارات» مرة واحدة في `/users` |
| M2 | مغلق بالبذرة | `WorkShift`=1 وعمود «صباحية» في الحضور |
| M3 | مغلق بالبذرة | أسماء مشاريع عربية في `/projects` |

---

## عدّادات بعد البذرة (مرجع)

```
ExpenseRequest: 6 | ExpenseCategory: 7
EvaluationCycle: 1 | EmployeeEvaluation: 6 | PeriodicEvaluation: 4
MeetingAmendment: 1 | Meetings agenda filled: 4/4
WorkShift: 1 | Project: 3 | ProjectVisit: 5
```

---

## مدخلات المرحلة 3 (أولوية)

1. **A1** — تمرير `title` من مكوّنات Livewire التي تُرجع `view()` بلا تخطيط بعنوان.
2. **A8** — مسار `GET /logout` يعيد توجيهًا آمنًا (أو يُنهي الجلسة ثم يحوّل).
3. **A7** — `title` عربي على حقول `/login` المطلوبة.
4. **صرف (مستجد)** — افتراضي تبويب «جميع الطلبات» لمن يملك `finance.expenses.view`.
5. تأكيد اختبارات `H1`/`H2` إن نقصت.
