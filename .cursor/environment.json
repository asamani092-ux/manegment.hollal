{
  "name": "hollal-platform",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "cd hollal-platform && composer install --no-interaction && (test -f .env || cp .env.example .env) && php artisan key:generate --force && touch database/database.sqlite && php artisan migrate --force --no-interaction && php artisan db:seed --force --no-interaction",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "Laravel",
      "command": "bash .cursor/start.sh && tail -f /tmp/laravel-serve.log",
      "description": "Hollal management platform (Arabic RTL UI)"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8000
    }
  ]
}
